spring:
  profiles:
    include: thirdparty-mp-sdk-dev
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/jlyzpart?characterEncoding=utf-8&&allowMultiQueries=true&&autoReconnect=true&failOverReadOnly=false
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: ENC(5c0Osk6LlIJEgirFZyZj7Q==)
  thymeleaf:
    prefix: classpath:/templates/
mybatis:
    type-aliases-package: tk.mybatis.springboot.model
    mapper-locations: classpath:mapper/*.xml

mapper:
    mappers:
        - tk.mybatis.springboot.util.MyMapper
    not-empty: false
    identity: MYSQL

pagehelper:
    helperDialect: mysql
    reasonable: true
    supportMethodsArguments: true
    params: count=countSql